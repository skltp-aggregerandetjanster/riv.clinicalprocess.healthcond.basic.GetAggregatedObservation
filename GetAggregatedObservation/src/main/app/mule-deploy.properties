# Mule Application Deployment Descriptor
redeployment.enabled=true
encoding=UTF-8

# Only load GetAggregatedObservation-common.xml, it will import the other config files since mule-deploy.properties can't load config-files from jar-files on the classpath, e.g. agp-core.jar
# config.resources=GetAggregatedObservation-common.xml,aggregating-services-common.xml,aggregating-service.xml,process-notification-service.xml
config.resources=GetAggregatedObservation-common.xml

domain=default
